#pragma once
////////////////////////////////////////////////////////////////////////////////////////
//	   File: DownloadBuffer.h
////////////////////////////////////////////////////////////////////////////////////////
// Description:
//
// A buffer used when downloading data. The buffer is filled in the background but can
// be accessed before it is completely filled.
//
////////////////////////////////////////////////////////////////////////////////////////

#include "juce.h"

class DownloadBuffer
{
public:
	DownloadBuffer(void);
	~DownloadBuffer(void);

private:
	CriticalSection	_lock;

};
